# homepage

Personal research portfolio site — static HTML/CSS, deployed with Cloudflare Pages.

## Local preview

```bash
python3 -m http.server 8000
# → http://localhost:8000
```

## Deploy

Connected to Cloudflare Pages (Git integration):

- Framework preset: **None**
- Build command: *(empty)*
- Build output directory: `/`

Custom domain: `venti0331.trade`
